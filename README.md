# Wrapper package for slug NPM package

It contains no unicode database for client,
so it won't increase the bundle size massively.

## Install

```sh
meteor add fuww:slug
```

## Usage

```js
slug('Some text');
```
